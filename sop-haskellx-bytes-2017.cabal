name: sop-haskellx-bytes
version: 2017
build-type: Simple
cabal-version: >= 1.10

library
  default-language:
    Haskell2010
  exposed-modules:
    SOP
  build-depends:
      base >= 4.9.1 && < 5
    , generics-sop >= 0.3 && < 0.4
    -- , postgresql-simple >= 0.5 && < 0.6
